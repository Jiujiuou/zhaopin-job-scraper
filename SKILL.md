---
name: zhaopin-job-scraper
description: |
  智联招聘岗位批量抓取与筛选工具。当用户想批量获取智联招聘岗位、
  按薪资/城市/公司黑名单/岗位类型筛选并生成 HTML 报告时使用。
  支持任意岗位类型（前端/后端/嵌入式/运营/产品等），只需用户提供
  岗位关键词、城市和薪资要求即可运行；包含词/排除词/黑名单为可选项。
  依赖 BrowserSkill (bsk CLI + Chrome 扩展) 和用户浏览器的登录态。
agent_created: true
---

# 智联招聘自动化 (zhaopin-job-scraper)

用用户的真实浏览器（带登录态）批量抓取智联招聘岗位，按条件过滤后生成
可直接点击跳转的 HTML 报告。**岗位类型无关**——前端、后端、嵌入式、
运营、产品等任何岗位都能用，只需调整配置。

## 何时使用

- 用户想批量获取某城市的岗位列表并筛选（薪资/黑名单/岗位类型）
- 用户嫌智联网页版逐个看太慢，想要一份带链接的岗位报告
- 用户想定期追踪某类岗位的供给情况

## 前置条件

1. **BrowserSkill 已安装**：`bsk` CLI 可用（`bsk --version`），Chrome 扩展已连接
2. **用户已在浏览器登录智联招聘**（`www.zhaopin.com` 登录态）
3. Python 3.8+

## 工作流程（AI 执行）

### 第 1 步：收集配置（问用户，尽量少问）

**必填**（只有这三项必须问）：
- **岗位关键词**：如「前端」「嵌入式」「产品运营」——让用户自己说，可多个
- **城市**：如「深圳」「广州」，或直接给智联城市 jl 编码（如 765）
- **薪资要求**：如「2 万以上」或「20K 以上」

**可选**（用户没提就用默认，不用追问）：
- 公司黑名单（默认：中软/软通/外企德科/华为/科锐等）
- 标题包含词 / 排除词（默认不启用，纯关键词抓取）
- 最大页数（默认 12 页/关键词）

生成 `config.json`（参考 `example/config_template.json`）。

### 第 2 步：连接浏览器

```bash
bsk session start          # 记下输出的 4 字母 session id
bsk tab list --session <id> --scope user   # 找智联标签页（或让用户打开）
bsk tab borrow <tab-id> --session <id>     # 借用智联标签页
```

### 第 3 步：运行抓取

```bash
python scripts/zhaopin_jobs.py --config config.json --session <session_id> \
  --bsk "D:/BrowserSkill/bin/bsk.exe"    # --bsk 按实际路径填
```

脚本会自动完成：明文关键词 → 智联自动加密 → 读 URL 编码 → 翻页抓取 → 过滤 → 生成 HTML。

### 第 4 步：交付

- 报告输出到 `output_dir`（默认当前目录），文件名含岗位/城市/薪资
- 用 `present_files` 展示报告，提醒用户抽查 2-3 个岗位验证薪资
- **必须** `bsk session stop <id>` 结束会话（即使出错也要停）

## 关键设计点（给 AI 的提示）

- **关键词自动加密**：智联搜索页支持 `?kw=明文关键词`，页面会重定向到
  加密编码 URL（如 `kwA96NLRO`）。脚本自动处理，AI 无需干预
- **薪资 4 种格式**：脚本已支持「万 / K / 元 / 面议」，过滤按换算后的 K 值
- **包含/排除词可选**：配置里留空数组 = 不启用该过滤，纯关键词抓取
- **报告结构**：岗位卡片含标题/薪资/公司/HR活跃度/可点击链接，按薪资或
  HR活跃度排序（`sort_by` 配置）

## 故障排查

| 问题 | 处理 |
|------|------|
| `bsk session not registered` | 会话超时，重新 `bsk session start` |
| 抓取 0 条 | 检查智联标签页是否登录、关键词是否有效 |
| 城市报错 | 城市名写中文（查 `cities.json`），或直接填 jl 编码 |
| 页面改版导致选择器失效 | 检查 `scripts/zhaopin_jobs.py` 顶部的 DOM 选择器常量 |

## Red Lines

1. **不做全自动爬虫**：依赖用户浏览器登录态，始终以"人机协作"方式运行，
   每次会话由用户发起
2. **遵守智联协议**：控制抓取频率（脚本已内置 1-3 秒间隔），仅用于个人求职
3. **不绕过登录/验证码**：遇到验证码用 `bsk request-help` 请用户处理
4. **结束后停止会话**：`bsk session stop <id>`，避免残留 Agent 窗口
